(function () {
    'use strict';

    // Copy this file to auth-config.js and fill in your Supabase project values.
    // Get these from: Supabase Dashboard → Settings → API
    // NEVER commit auth-config.js to git.
    window.WW_AUTH_CONFIG = {
        supabaseUrl: 'https://YOUR_PROJECT_ID.supabase.co',
        supabaseAnonKey: 'YOUR_PUBLISHABLE_ANON_KEY'
    };
})();
